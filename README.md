Using Yogathon Tracker:

To use the the Yogathon Tracker/Workouttracker app,  clone the repo. Then run bundle install.

After that, run rake db:migrate and then run shotgun.

Everything should then be good to go!
